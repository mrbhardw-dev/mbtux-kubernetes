apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: cluster-secret-store
spec:
  provider:
    kubernetes:
      remoteNamespace: eso-secrets
      server:
        url: https://kubernetes.default.svc
      auth:
        token:
          bearerToken:
            name: external-secrets
            key: token
