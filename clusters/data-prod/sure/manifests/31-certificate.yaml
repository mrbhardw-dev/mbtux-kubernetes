apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sure-tls
  namespace: sure
spec:
  dnsNames:
    - sure.mbtux.com
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: letsencrypt-prod
  secretName: sure-tls
  duration: 2160h
  renewBefore: 360h