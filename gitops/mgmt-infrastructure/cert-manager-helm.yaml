apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mgmt-infrastructure-cert-manager
  namespace: argocd
  labels:
    app.kubernetes.io/name: mgmt-infrastructure-cert-manager
    app.kubernetes.io/part-of: mbtux-platform
    app.kubernetes.io/managed-by: argocd
    platform.tier: management
    app.kubernetes.io/component: infrastructure
spec:
  project: mgmt-infrastructure

  source:
    repoURL: https://charts.jetstack.io
    chart: cert-manager
    targetRevision: v1.17.0
    helm:
      releaseName: cert-manager
      values: |
        installCRDs: true
        nodeSelector:
          kubernetes.io/os: linux
        dns01RecursiveNameservers: "1.1.1.1:53,9.9.9.9:53"
        dns01RecursiveNameserversList:
          - 1.1.1.1:53
          - 9.9.9.9:53

  destination:
    server: https://kubernetes.default.svc
    namespace: cert-manager

  syncPolicy:
    automated: null
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
