apiVersion: v1
kind: Secret
metadata:
  name: authentik-oidc-client
  namespace: coder
type: Opaque
data:
  # IMPORTANT: These are PLACEHOLDER values. Replace with actual base64-encoded
  # credentials from your Authentik OIDC provider configuration.
  #
  # After creating OIDC provider in Authentik, generate proper values:
  #   CLIENT_ID_B64=$(echo -n "YOUR_ACTUAL_CLIENT_ID" | base64 -w0)
  #   CLIENT_SECRET_B64=$(echo -n "YOUR_ACTUAL_CLIENT_SECRET" | base64 -w0)
  #
  # Then update this file with those base64 strings.
  client-id: UExBTlRfQ0xJUV9DTElWRVJfQUJMT1dF  # placeholder: REPLACE_CLIENT_ID_BASE64
  client-secret: UExBTlRfQ0xJUV9TRUNSRVRTX0FCTE9XRS1QT1NUX0JBU0U2NA==  # placeholder: REPLACE_CLIENT_SECRET_BASE64
