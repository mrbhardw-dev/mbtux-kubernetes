apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tls-authentik
  namespace: authentik
spec:
  secretName: tls-authentik
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - authentik.mbtux.com
