apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: authentik-https-redirect
  namespace: authentik
spec:
  redirectScheme:
    scheme: https
    permanent: true
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: authentik-http-redirect
  namespace: authentik
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`authentik.mbtux.com`)
      middlewares:
        - name: authentik-https-redirect
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: authentik
  namespace: authentik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`authentik.mbtux.com`) && PathPrefix(`/outpost.goauthentik.io`)
      priority: 11
      services:
        - name: authentik-server
          port: 80
    - kind: Rule
      match: Host(`authentik.mbtux.com`)
      priority: 10
      services:
        - name: authentik-server
          port: 80
  tls:
    secretName: tls-authentik
