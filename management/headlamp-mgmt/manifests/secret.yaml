apiVersion: v1
kind: Secret
metadata:
  name: headlamp-admin-secret
  namespace: headlamp-system
type: Opaque
stringData:
  # This secret will store the initial admin token after deployment
  # Run: kubectl create token headlamp-admin -n headlamp-system
  # to get the token for login