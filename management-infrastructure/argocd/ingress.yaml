## DEPRECATED: Ingress removed because it conflicts with IngressRoute (ingressroute.yaml)
# The standard K8s Ingress doesn't support gRPC properly, causing the ArgoCD
# web UI to fail after login (welcome page shows but API calls via gRPC fail).
# Keep only ingressroute.yaml which has proper h2c/gRPC support.
#
# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: argocd-server
#   namespace: argocd
#   annotations:
#     kubernetes.io/ingress.class: traefik
#     cert-manager.io/cluster-issuer: letsencrypt-prod
#     traefik.ingress.kubernetes.io/router.entrypoints: websecure
#     traefik.ingress.kubernetes.io/pass-host-header: "true"
# spec:
#   ingressClassName: traefik
#   tls:
#     - hosts:
#         - argocd-mgmt.mbtux.com
#       secretName: tls-argocd-server
#   rules:
#     - host: argocd-mgmt.mbtux.com
#       http:
#         paths:
#           - path: /
#             pathType: Prefix
#             backend:
#               service:
#                 name: argocd-server
#                 port:
#                   number: 80