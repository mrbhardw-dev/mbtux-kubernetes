apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tls-traefik-dashboard
  namespace: traefik
spec:
  secretName: tls-traefik-dashboard
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - traefik-mgmt.mbtux.com