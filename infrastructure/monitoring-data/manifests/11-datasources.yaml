apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-datasources
  namespace: monitoring
data:
  datasources.yaml: |
    apiVersion: 1
    
    datasources:
      - name: Prometheus (data-cluster)
        type: prometheus
        access: proxy
        url: http://prometheus.data.mbtux.com
        isDefault: true
        editable: true
        jsonData:
          httpMethod: POST
          timeInterval: 30s
      - name: Prometheus (mgmt-cluster)
        type: prometheus
        access: proxy
        url: http://prometheus.mgmt.mbtux.com
        editable: true
        jsonData:
          httpMethod: POST
          timeInterval: 30s