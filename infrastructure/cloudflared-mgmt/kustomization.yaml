apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cloudflared-mgmt

resources:
  - namespace.yaml
  - secret.yaml
  - deployment.yaml

commonLabels:
  app.kubernetes.io/name: cloudflared-mgmt
  app.kubernetes.io/part-of: infrastructure
  managed-by: fleetcd
