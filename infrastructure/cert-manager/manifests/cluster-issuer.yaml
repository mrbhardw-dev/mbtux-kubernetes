apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: vault-pki-issuer
spec:
  vault:
    server: http://192.168.0.20:8200
    path: pki-int
    auth:
      tokenSecretRef:
        name: vault-token
        key: token