# Cloudflare Tunnel Configuration
# This configmap contains the tunnel configuration for all services
# Tunnel ID: 52cce491-b5b2-4c6c-991c-25839733a4b3
# Domain: mbtux.com
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: cloudflared-config
  namespace: cloudflared
data:
  config.yaml: |
    tunnel: 52cce491-b5b2-4c6c-991c-25839733a4b3
    protocol: http2
    
    # Ingress rules for all services
    # All traffic goes to the Nginx Ingress Controller at 192.168.0.210
    ingress:
      # ACME HTTP01 challenge - must be HTTP for Let's Encrypt
      - hostname: "*.mbtux.com"
        service: http://192.168.0.210:80
        originRequest:
          connectTimeout: 60s
        pathMatch: "/.well-known/acme-challenge/*"
      
      # ArgoCD - argo.mbtux.com
      - hostname: argo.mbtux.com
        service: http://192.168.0.210:80
        originRequest:
          connectTimeout: 60s
      
      
      # Longhorn - longhorn.mbtux.com
      - hostname: longhorn.mbtux.com
        service: http://192.168.0.210:80
        originRequest:
          connectTimeout: 60s
      
      # Codecombat - codecombat.mbtux.com
      - hostname: codecombat.mbtux.com
        service: http://192.168.0.210:80
        originRequest:
          connectTimeout: 60s
      
      # Catch-all rule - route to Nginx Ingress Controller
      - service: http://192.168.0.210:80
        originRequest:
          connectTimeout: 60s
